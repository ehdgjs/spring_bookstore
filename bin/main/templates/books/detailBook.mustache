{{>layout/header}}
{{>layout/nav}}
<script src="/js/app/detailBook.js"></script>

{{#bookInfo}}
{{#master}}
    <div class="d-flex justify-content-end col-md-11">
        <button type="button" id="btn-booksDelete" class="btn btn-lg btn-secondary">삭제</button>
    </div>
{{/master}}
{{^master}}
{{/master}}
<main role="main" class="container">
    <div class="row">
        <div class="col-md-8 blog-main">
            <h3 class="pb-3 mb-4 border-bottom">
                책 정보
            </h3>
            <div class="blog-post">
                <h2 class="blog-post-title">{{bookName}}</h2>
                <div class="ml-1 row d-flex justify-content-between">
                    <p class="blog-post-meta"><strong>작가</strong> : {{bookAuthor}}</p>
                    <p class="blog-post-meta"><strong>출판사</strong> : {{bookPublish}}</p>
                </div>

                <div class="d-flex justify-content-end">
                    <h4 id="book_Price">{{bookPrice}}</h4><h3>원</h3>
                </div>
                <hr>
                <p>
                    {{bookDetail}}
                </p>


            </div>
        </div>
        <aside class="col-md-4 blog-sidebar">
            <div class="p-3 mb-3 bg-light rounded">
                <h4 class="mb-2">주문</h4>
                <hr>
                <form>
                    <div class="mb-2 row d-flex justify-content-end">
                        <input type="number" id="count" value="1" min="1" max="{{bookCount}}"/>
                        <h4>권</h4>
                    </div>
                    <div id="total" class="row d-flex flex-row-reverse">
                        <h4>원</h4>
                    </div>
                </form>
                {{#existSession}}
                    <div class="btn-group-lg-vertical">
                        <button class="btn btn-outline-secondary btn-lg btn-block" disabled id="btn-addCart">장바구니 추가</button>
                        <button class="btn btn-outline-secondary btn-lg btn-block" disabled>주문하기</button>
                    </div>
                {{/existSession}}
                {{^existSession}}
                    <div class="btn-group-lg-vertical">
                        <button class="btn btn-outline-secondary btn-lg btn-block" id="btn-addCart">장바구니 추가</button>
                        <button class="btn btn-outline-secondary btn-lg btn-block" onclick=orderBtn()>주문하기</button>
                    </div>
                {{/existSession}}
            </div>
        </aside>
    </div>
    {{/bookInfo}}
</main>

{{>layout/footer}}